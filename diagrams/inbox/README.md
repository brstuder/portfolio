# Diagram Inbox

Drop a reference mockup here (Excalidraw PNG or SVG export) to have a diagram built
from it instead of a text/YAML description. See `.claude/skills/architecture-diagram/SKILL.md`
→ "Working from a Reference Mockup" for the workflow.

Files here are temporary — they get deleted after a successful build, but only with
your explicit confirmation, never automatically.
